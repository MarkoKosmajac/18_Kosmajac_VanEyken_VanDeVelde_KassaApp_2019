/*package testpack;


import javafx.application.*;
import javafx.stage.*;
import javafx.scene.*;
import model.ArtikelCompany;
import model.UITestStory1;

public class Main2 extends Application {
    public static void main(String[] args)    {
        launch(args);
    }

    @Override
    public void start(Stage primaryStage) {
        ArtikelCompany artikelCompany = new ArtikelCompany();
        ArtikelOverview view = new ArtikelOverview(artikelCompany);

        Scene mainScene = new Scene(view);

        primaryStage.setTitle("Artikel Overview");
        primaryStage.setScene(mainScene);
        primaryStage.show();

    }
}*/